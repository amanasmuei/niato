# Required: API key for Claude (https://console.anthropic.com/)
ANTHROPIC_API_KEY=

# Optional: log level for the console logger. One of: debug | info | warn | error.
# Defaults to "info" if unset. Leave commented to use the default — an empty
# string would fail enum validation since zod's .default() only fires on undefined.
